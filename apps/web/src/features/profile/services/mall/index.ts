import { ShoppingBag } from "lucide-react";
import type { ProfileService } from "../types";

export const mallService: ProfileService = {
  action: "share",
  helper: "Public shop and share links",
  icon: ShoppingBag,
  id: "mall",
  label: "Mall",
};
