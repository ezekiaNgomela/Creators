import { Store } from "lucide-react";
import type { ProfileService } from "../types";

export const itemStoreService: ProfileService = {
  action: "create",
  helper: "Products and creator drops",
  icon: Store,
  id: "item-store",
  label: "Item Store",
};
